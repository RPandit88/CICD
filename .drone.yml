kind: pipeline
type: docker
name: Testing Python CI/CD


steps:
- name: Install and deploy
  image: python
  environment:
    cisco:
      from_secret: cisco
  commands:
  - pip3 install -r requirements.txt
  - python3 check.py

trigger:
  branch:
    exclude:
    - main
    - master
